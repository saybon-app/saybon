@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
*{
  box-sizing:border-box;
  margin:0;
  padding:0;
}

html,body{
  width:100%;
  min-height:100%;
  font-family:'Inter',sans-serif;
  background:
    radial-gradient(circle at center, rgba(0,180,255,.18) 0%, rgba(0,0,0,0) 28%),
    linear-gradient(180deg,#04060c 0%,#08111d 42%,#071827 100%);
  color:#ffffff;
  overflow-x:hidden;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

body{
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:34px 18px;
}

.hidden{
  display:none !important;
}

/* ==========================================
   SHELL
========================================== */

.placement-shell{
  width:100%;
  max-width:980px;
  margin:0 auto;
}

.progress-wrap{
  width:100%;
  margin-bottom:26px;
}

.progress-track{
  width:100%;
  height:10px;
  border-radius:999px;
  background:rgba(255,255,255,0.08);
  overflow:hidden;
  box-shadow:inset 0 0 0 1px rgba(255,255,255,0.06);
}

.progress-bar{
  width:0%;
  height:100%;
  border-radius:999px;
  background:linear-gradient(90deg,#42e9f5,#5bbdff);
  transition:width .35s ease;
  box-shadow:0 0 16px rgba(66,233,245,.35);
}

.progress-meta{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-top:12px;
  gap:12px;
  flex-wrap:wrap;
}

#levelBadge,
#questionCounter{
  font-size:.92rem;
  font-weight:600;
  color:rgba(255,255,255,.78);
  letter-spacing:.02em;
}

/* ==========================================
   MAIN CARD
========================================== */

.placement-card{
  width:100%;
  min-height:72vh;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:24px 12px;
}

.question-stage{
  width:100%;
  max-width:760px;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:22px;
  text-align:center;
}

.skill-tag{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-height:36px;
  padding:0 16px;
  border-radius:999px;
  background:rgba(255,255,255,0.08);
  border:1px solid rgba(255,255,255,0.10);
  color:#bdefff;
  font-size:.85rem;
  font-weight:700;
  letter-spacing:.08em;
  text-transform:uppercase;
}

#questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

/* ==========================================
   MEDIA
========================================== */

.media-area{
  width:100%;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:16px;
}

.audio-wrap{
  display:flex;
  justify-content:center;
  align-items:center;
}

.audio-wrap audio{
  width:min(350px,92vw);
  border-radius:999px;
  background:#fff;
  padding:5px 10px;
  box-shadow:0 12px 28px rgba(0,0,0,.18);
}

.reading-block{
  width:100%;
  max-width:700px;
  background:rgba(255,255,255,0.08);
  border:1px solid rgba(255,255,255,0.12);
  border-radius:24px;
  padding:22px 24px;
  text-align:left;
  backdrop-filter:blur(16px);
  box-shadow:0 16px 36px rgba(0,0,0,.16);
}

.reading-label{
  font-size:.82rem;
  font-weight:800;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:#bfefff;
  margin-bottom:12px;
}

.reading-text{
  font-size:1.08rem;
  line-height:1.9;
  color:#ffffff;
}

.reading-question{
  width:100%;
  max-width:700px;
  font-size:1.05rem;
  line-height:1.75;
  color:#eefbff;
  font-weight:600;
  margin-top:-2px;
}

/* ==========================================
   ANSWERS
========================================== */

.answers{
  width:100%;
  display:flex;
  flex-direction:column;
  gap:14px;
  align-items:center;
}

.answer-pill{
  width:100%;
  max-width:520px;
  min-height:52px;
  border:none;
  border-radius:999px;
  background:linear-gradient(180deg, rgba(243,250,255,.98), rgba(228,245,255,.94));
  color:#18384d;
  font-size:.95rem;
  font-weight:700;
  padding:12px 20px;
  cursor:pointer;
  box-shadow:
    0 10px 22px rgba(0,0,0,.14),
    inset 0 1px 0 rgba(255,255,255,.7);
  transition:all .18s ease;
}

.answer-pill:hover{
  transform:translateY(-2px) scale(1.01);
}

.answer-pill:active{
  transform:translateY(0);
}

/* ==========================================
   WRITING / SPEAKING
========================================== */

.response-card{
  width:100%;
  max-width:700px;
  background:rgba(255,255,255,0.08);
  border:1px solid rgba(255,255,255,0.12);
  border-radius:28px;
  padding:24px 24px 22px;
  backdrop-filter:blur(18px);
  box-shadow:0 18px 40px rgba(0,0,0,.16);
  text-align:left;
}

.response-prompt{
  font-size:1.04rem;
  line-height:1.8;
  color:#ffffff;
  font-weight:600;
  margin-bottom:16px;
}

.response-box{
  width:100%;
  min-height:150px;
  border:none;
  outline:none;
  resize:vertical;
  border-radius:20px;
  padding:18px 18px;
  font-size:.98rem;
  line-height:1.75;
  font-family:'Inter',sans-serif;
  background:rgba(255,255,255,.97);
  color:#17354d;
  box-shadow:inset 0 1px 2px rgba(0,0,0,.05);
}

.submit-pill{
  margin-top:14px;
}

.record-panel{
  display:flex;
  flex-direction:column;
  gap:16px;
}

.record-actions{
  display:flex;
  flex-wrap:wrap;
  gap:12px;
}

.record-btn,
.stop-btn{
  min-width:160px;
  min-height:52px;
  border:none;
  border-radius:999px;
  font-size:.96rem;
  font-weight:700;
  cursor:pointer;
  padding:0 20px;
  transition:transform .16s ease, box-shadow .16s ease;
}

.record-btn{
  background:linear-gradient(90deg,#53ecff,#6cc8ff);
  color:#07243b;
  box-shadow:0 14px 28px rgba(83,236,255,.20);
}

.stop-btn{
  background:rgba(255,255,255,0.12);
  color:#ffffff;
  border:1px solid rgba(255,255,255,0.16);
}

.record-btn:hover,
.stop-btn:hover{
  transform:translateY(-2px);
}

.record-status{
  font-size:.96rem;
  color:#d7f7ff;
  font-weight:600;
}

.recorded-audio{
  width:100%;
  margin-top:6px;
}

/* ==========================================
   INTERVENTION
========================================== */

.intervention{
  position:fixed;
  inset:0;
  display:flex;
  align-items:center;
  justify-content:center;
  background:rgba(2,8,20,.72);
  backdrop-filter:blur(14px);
  z-index:50;
  padding:24px;
}

.intervention-card{
  width:100%;
  max-width:620px;
  background:rgba(255,255,255,0.08);
  border:1px solid rgba(255,255,255,0.12);
  border-radius:32px;
  padding:36px 28px 30px;
  text-align:center;
  backdrop-filter:blur(20px);
  box-shadow:0 24px 56px rgba(0,0,0,.28);
}

.intervention-card h2{
  font-family:'Playfair Display',serif;
  font-size:2rem;
  margin-bottom:12px;
}

.intervention-card p{
  font-size:1rem;
  line-height:1.8;
  color:#eefbff;
  max-width:520px;
  margin:0 auto 24px;
}

.intervention-actions{
  display:flex;
  flex-wrap:wrap;
  justify-content:center;
  gap:14px;
}

.pill-btn{
  min-width:220px;
  min-height:58px;
  border:none;
  border-radius:999px;
  font-size:.98rem;
  font-weight:700;
  cursor:pointer;
  transition:transform .16s ease, box-shadow .16s ease;
}

.pill-btn:hover{
  transform:translateY(-2px);
}

.continue-btn{
  background:linear-gradient(90deg,#4fe8ff,#6bc7ff);
  color:#07243b;
  box-shadow:0 16px 32px rgba(79,232,255,.22);
}

.reveal-btn{
  background:rgba(255,255,255,0.12);
  color:#ffffff;
  border:1px solid rgba(255,255,255,0.18);
}

/* ==========================================
   RESULT PAGE SHARED
========================================== */

.result-shell{
  width:100%;
  max-width:760px;
  margin:0 auto;
}

.result-box{
  width:100%;
  background:rgba(255,255,255,0.08);
  border:1px solid rgba(255,255,255,0.12);
  border-radius:34px;
  padding:42px 28px 34px;
  text-align:center;
  backdrop-filter:blur(20px);
  box-shadow:0 26px 60px rgba(0,0,0,.26);
}

.result-box h1{
  font-family:'Playfair Display',serif;
  font-size:2.25rem;
  margin-bottom:14px;
}

.result-main{
  font-size:1.15rem;
  line-height:1.8;
  color:#eefbff;
  margin-bottom:26px;
}

.result-level-badge{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-height:54px;
  padding:0 24px;
  border-radius:999px;
  background:linear-gradient(90deg,#4fe8ff,#6bc7ff);
  color:#07243b;
  font-size:1.08rem;
  font-weight:800;
  margin-bottom:24px;
}

.result-meta{
  display:flex;
  flex-wrap:wrap;
  gap:12px;
  justify-content:center;
  margin-bottom:28px;
}

.result-chip{
  min-height:46px;
  padding:0 18px;
  border-radius:999px;
  background:rgba(255,255,255,0.10);
  border:1px solid rgba(255,255,255,0.12);
  display:inline-flex;
  align-items:center;
  justify-content:center;
  color:#e8f8ff;
  font-size:.95rem;
  font-weight:600;
}

.result-actions{
  display:flex;
  justify-content:center;
  flex-wrap:wrap;
  gap:14px;
}

/* ==========================================
   MOBILE
========================================== */

@media (max-width:768px){
  body{
    padding:24px 14px;
  }

  .placement-card{
    min-height:auto;
    padding:20px 8px;
  }

  .question-stage{
    gap:20px;
  }

  #questionText{
    font-size:1.9rem;
    line-height:1.22;
    max-width:92vw;
  }

  .answer-pill{
    min-height:64px;
    font-size:1rem;
    padding:0 18px;
  }

  .reading-block,
  .response-card{
    padding:22px 18px;
    border-radius:24px;
  }

  .reading-text,
  .reading-question,
  .response-prompt{
    font-size:1rem;
  }

  .response-box{
    min-height:150px;
    font-size:.98rem;
  }

  .pill-btn{
    width:100%;
    min-width:unset;
  }
}

  .placement-card{
    min-height:auto;
    padding:20px 8px;
  }

  .question-stage{
    gap:20px;
  }

  #questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

  .answer-pill{
    min-height:64px;
    font-size:1rem;
    padding:0 18px;
  }

  .reading-block,
  .response-card{
    padding:22px 18px;
    border-radius:24px;
  }

  .reading-text,
  .reading-question,
  .response-prompt{
    font-size:1rem;
  }

  .response-box{
    min-height:150px;
    font-size:.98rem;
  }

  .pill-btn{
    width:100%;
    min-width:unset;
  }
}

  .placement-card{
    min-height:auto;
    padding:18px 6px;
  }

  .question-stage{
    gap:18px;
  }

  #questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

  .answer-pill{
  width:100%;
  max-width:520px;
  min-height:52px;
  border:none;
  border-radius:999px;
  background:linear-gradient(180deg, rgba(243,250,255,.98), rgba(228,245,255,.94));
  color:#18384d;
  font-size:.95rem;
  font-weight:700;
  padding:12px 20px;
  cursor:pointer;
  box-shadow:
    0 10px 22px rgba(0,0,0,.14),
    inset 0 1px 0 rgba(255,255,255,.7);
  transition:all .18s ease;
}

  .reading-block,
  .response-card{
    padding:20px 18px;
    border-radius:22px;
  }

  .reading-text,
  .reading-question,
  .response-prompt{
    font-size:.98rem;
  }

  .response-box{
    min-height:140px;
    font-size:.96rem;
  }

  .pill-btn,
  .record-btn,
  .stop-btn{
    width:100%;
    min-width:unset;
  }

  .result-box{
    padding:34px 20px 28px;
    border-radius:28px;
  }

  .result-box h1{
    font-size:1.9rem;
  }
}




.skill-badge,
[class*="skill-badge"],
[class~="skill-badge"]{
  display:none !important;
  visibility:hidden !important;
  opacity:0 !important;
  width:0 !important;
  height:0 !important;
  margin:0 !important;
  padding:0 !important;
  overflow:hidden !important;
  pointer-events:none !important;
}



/* === READING LAYOUT OVERRIDE START === */

/* Give reading questions more breathing room */
body.reading-mode .question-stage,
.placement-shell.reading-mode .question-stage{
  gap: 34px !important;
  max-width: 980px !important;
  padding-top: 14px !important;
}

/* Slightly reduce huge question heading for reading pages */
body.reading-mode #questionText,
.placement-shell.reading-mode #questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

/* Reading block should breathe more */
body.reading-mode .reading-block,
.placement-shell.reading-mode .reading-block{
  max-width: 820px !important;
  padding: 30px 34px !important;
  border-radius: 28px !important;
  margin-top: 8px !important;
  margin-bottom: 8px !important;
}

/* TEXT label spacing */
body.reading-mode .reading-label,
.placement-shell.reading-mode .reading-label{
  margin-bottom: 16px !important;
  font-size: 0.95rem !important;
  letter-spacing: 0.12em !important;
}

/* Passage readability */
body.reading-mode .reading-text,
.placement-shell.reading-mode .reading-text{
  font-size: 1.28rem !important;
  line-height: 2.05 !important;
  letter-spacing: 0.01em !important;
}

/* Question under passage */
body.reading-mode .reading-question,
.placement-shell.reading-mode .reading-question{
  max-width: 760px !important;
  font-size: 1.22rem !important;
  line-height: 1.8 !important;
  margin-top: 4px !important;
  margin-bottom: 8px !important;
}

/* Narrower pills for reading pages */
body.reading-mode .answers,
.placement-shell.reading-mode .answers{
  gap: 16px !important;
  max-width: 760px !important;
}

body.reading-mode .answer-pill,
.placement-shell.reading-mode .answer-pill{
  max-width: 620px !important;
  min-height: 64px !important;
  font-size: 1.03rem !important;
  padding: 0 22px !important;
}

/* Desktop reading layout sits a bit lower and calmer */
@media (min-width: 769px){
  body.reading-mode .placement-card,
  .placement-shell.reading-mode .placement-card{
    padding-top: 36px !important;
    padding-bottom: 42px !important;
  }
}

/* Mobile reading screens */
@media (max-width:768px){
  body{
    padding:24px 14px;
  }

  .placement-card{
    min-height:auto;
    padding:20px 8px;
  }

  .question-stage{
    gap:20px;
  }

  #questionText{
    font-size:1.9rem;
    line-height:1.22;
    max-width:92vw;
  }

  .answer-pill{
    min-height:64px;
    font-size:1rem;
    padding:0 18px;
  }

  .reading-block,
  .response-card{
    padding:22px 18px;
    border-radius:24px;
  }

  .reading-text,
  .reading-question,
  .response-prompt{
    font-size:1rem;
  }

  .response-box{
    min-height:150px;
    font-size:.98rem;
  }

  .pill-btn{
    width:100%;
    min-width:unset;
  }
}

  .placement-card{
    min-height:auto;
    padding:20px 8px;
  }

  .question-stage{
    gap:20px;
  }

  #questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

  .answer-pill{
    min-height:64px;
    font-size:1rem;
    padding:0 18px;
  }

  .reading-block,
  .response-card{
    padding:22px 18px;
    border-radius:24px;
  }

  .reading-text,
  .reading-question,
  .response-prompt{
    font-size:1rem;
  }

  .response-box{
    min-height:150px;
    font-size:.98rem;
  }

  .pill-btn{
    width:100%;
    min-width:unset;
  }
}

  body.reading-mode #questionText,
  .placement-shell.reading-mode #questionText{
  font-family:'Nunito', system-ui, sans-serif;
  font-size:clamp(2rem, 3.3vw, 3rem);
  font-weight:800;
  line-height:1.16;
  letter-spacing:-0.035em;
  color:#ffffff;
  text-shadow:0 2px 14px rgba(0,0,0,.22);
  margin-bottom:6px;
  max-width:860px;
}

  body.reading-mode .reading-block,
  .placement-shell.reading-mode .reading-block{
    padding: 22px 20px !important;
    border-radius: 22px !important;
  }

  body.reading-mode .reading-text,
  .placement-shell.reading-mode .reading-text{
    font-size: 1.06rem !important;
    line-height: 1.9 !important;
  }

  body.reading-mode .reading-question,
  .placement-shell.reading-mode .reading-question{
    font-size: 1.02rem !important;
    line-height: 1.65 !important;
  }

  body.reading-mode .answer-pill,
  .placement-shell.reading-mode .answer-pill{
    max-width: 100% !important;
    min-height: 60px !important;
    font-size: 0.98rem !important;
  }
}

/* === READING LAYOUT OVERRIDE END === */





/* === RECORDING NOTE OVERRIDE START === */

.recording-note,
.speaking-note,
.voice-note,
.recording-helper{
  font-size:0.9rem !important;
  font-weight:500 !important;
  line-height:1.5 !important;
  text-align:center !important;
  color:rgba(255,255,255,0.58) !important;
  letter-spacing:0.01em !important;
  margin-top:6px !important;
  margin-bottom:10px !important;
  width:100% !important;
  display:block !important;
}

/* fallback if note is just a p inside speaking card */
.response-card p.recording-note,
.response-card p.speaking-note,
.response-card p.voice-note,
.response-card p.recording-helper{
  text-align:center !important;
  color:rgba(255,255,255,0.58) !important;
  font-size:0.9rem !important;
  font-weight:500 !important;
}

/* Mobile */
@media (max-width:768px){
  .recording-note,
  .speaking-note,
  .voice-note,
  .recording-helper{
    font-size:0.82rem !important;
    line-height:1.45 !important;
    margin-top:4px !important;
    margin-bottom:8px !important;
  }
}

/* === RECORDING NOTE OVERRIDE END === */



/* === QUESTION HEADING OVERRIDE START === */

/* Main question text across placement engine */
#questionText{
  font-size:2.45rem !important;
  font-weight:700 !important;
  line-height:1.24 !important;
  letter-spacing:-0.02em !important;
  max-width:820px !important;
  margin:0 auto 10px !important;
  color:#f7fbff !important;
  text-shadow:0 2px 12px rgba(0,0,0,.18) !important;
}

/* Reading / writing / speaking sub-prompts under the main title */
.response-prompt,
.reading-question{
  font-size:1.04rem !important;
  font-weight:600 !important;
  line-height:1.65 !important;
  letter-spacing:0.005em !important;
  color:rgba(255,255,255,.92) !important;
}

/* Reading text itself */
.reading-text{
  font-size:1.08rem !important;
  font-weight:500 !important;
  line-height:1.82 !important;
  letter-spacing:0.003em !important;
  color:#ffffff !important;
}

/* Optional writing/speaking prompt inside cards */
.response-card h3,
.response-card h4{
  font-size:1.08rem !important;
  font-weight:600 !important;
  line-height:1.55 !important;
  letter-spacing:0.01em !important;
}

/* Tablet */
@media (max-width:1024px){
  #questionText{
    font-size:2.15rem !important;
    line-height:1.26 !important;
    max-width:760px !important;
  }
}

/* Mobile */
@media (max-width:768px){
  #questionText{
    font-size:1.72rem !important;
    font-weight:700 !important;
    line-height:1.30 !important;
    letter-spacing:-0.015em !important;
    max-width:100% !important;
    margin:0 auto 8px !important;
  }

  .response-prompt,
  .reading-question{
    font-size:0.97rem !important;
    line-height:1.58 !important;
  }

  .reading-text{
    font-size:1rem !important;
    line-height:1.72 !important;
  }
}

/* Small laptop / tighter viewport */
@media (max-height:780px){
  #questionText{
    font-size:2.05rem !important;
    line-height:1.22 !important;
  }
}

/* === QUESTION HEADING OVERRIDE END === */



/* === RECORDING HINT OVERRIDE START === */

.recording-instruction,
.recording-hint,
.tap-recording-hint,
.voice-hint,
.response-hint{
  text-align:center !important;
  font-size:0.84rem !important;
  font-weight:400 !important;
  line-height:1.45 !important;
  letter-spacing:0.01em !important;
  color:rgba(255,255,255,0.58) !important;
  width:100% !important;
  margin:10px auto 8px !important;
  display:block !important;
}

/* Mobile */
@media (max-width:768px){
  .recording-instruction,
  .recording-hint,
  .tap-recording-hint,
  .voice-hint,
  .response-hint{
    font-size:0.78rem !important;
    line-height:1.42 !important;
    margin:8px auto 6px !important;
  }
}

/* === RECORDING HINT OVERRIDE END === */
